version: '3.8'

services:
  app:
    build: .
    ports:
      # Option 1 architecture: We expose the port directly on VM 200
      - "8000:8000"
    volumes:
      # Binds the container's storage to your dedicated /var/www/ folder
      - /var/www/ytdown:/app/temp_storage
    restart: unless-stopped